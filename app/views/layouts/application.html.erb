<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag "posts.css" %>
</head>
      <p class="notice"><%= notice %></p>

      <p class="alert"><%= alert %></p>

  <div class="wrapper">
  <header class="header">
  <ul class="nav-ul">
    <li class="nav-li"><a href="/">BookFace</a></li>
    <% if @user %>
      <li class="nav-li">
      <%= link_to "View my profile", current_user %>
      </li>
      <li class="nav-li"  style="float:right"><a href="/logout" > Log Out </a></li>
      <% else %>
      <li class="nav-li"  style="float:right"><a href="/login" > Log In </a></li>
    <% end %>
    <li class="nav-li username" style="float:right"><%= @user ? "Logged in as: #{@user.first_name}" : '' %></li>
  </ul>
  </header>
  <body>
    <div class="left-side">
      <ul>
        <% if current_user %>
          <% if current_user.avatar.attached? %>
            <%= image_tag(url_for(@user.avatar), class: "profile_pic") %>
           
            <a href="#"><li class="sidebar-li">> Inbox</li></a>
          <% else %>
            <%= image_tag("profile_photo.png", class: "profile_pic")%>
           
            <a href="#"><li class="sidebar-li">> Inbox</li></a>
          <% end %>
        <% end %>
      </ul>
    </div>
  <main>
    <%= yield %>
  </main>
  <div class="right-side"></div>
    <footer>@2021-team-smiley-face</footer>
  </div>
</body>
</html>
